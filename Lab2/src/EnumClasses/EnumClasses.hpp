#pragma once

enum class State
{
    Dead,
    Alive,
};

enum class Errors{
    CallProgrammIncorrectly,
    ProblemWithInputOfIteration,
    Input,
    CanNotOpenFile,
    InputData,
};

enum class Mode{
    Offline,
    Online,
};

